# pra-ukl.soal1-sedang

menggunakan tipe data long agar bisa menghitung data dengan jumlah bnayak, menggunakan struktur if elss, if (n < 0) = print bilangan harus positif, else for ( int i = 1; i <= n; i++) = faktorial = faktorial * i;
